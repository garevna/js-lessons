{
  "s1.h1": "Promises",
  "s1.p1": "An asynchronous function call returns an instance of **⟦f0⟧**.",
  "s1.p2": "Let's recall some valuable properties of **⟦f0⟧** instances.",
  "s1.p3": "First of all, it is convenient that if an instance of **⟦f0⟧** goes to the **⟦f1⟧** state, it is not at all necessary to pick up the result immediately.",
  "s1.p4": "We can put our ‘magic box’ on a shelf:",
  "s1.p5": "and \"open\" it (like a tin can) when it suits us!<br><br>Moreover, the \"opener\" - the **⟦f2⟧** method - is always with it.<br><br>The **⟦f0⟧** instance is a reliable tin where the contents won't spoil or disappear.<br>The main thing is not to lose the reference to this can.",
  "s1.p6": "What else makes instances of **⟦f0⟧** so convenient for us?",
  "s1.p7": "The **⟦f0⟧** instance is a **microtask**, and microtasks have their own queue, the priority of which is higher than the priority of the task queue.<br><br>I.e. while the fat and sluggish tasks will stupidly sit in their queue and wait for the ‘office’ (**Call Stack**) to be vacated, microtasks one by one will jump past them into the ‘office’. And until the last microtask from the privileged queue passes through the **Call Stack**, the tasks will wait.",
  "s1.p8": "For example, as a result of executing the following code:",
  "s1.p9": "we'll see in the console:",
  "s1.p10": "<br>i.e. the asynchronous **⟦f3⟧** function behaves very modestly:<br>although all it does is just say hello,<br>but it doesn't break into the worker thread and yell \"Hello\" from the doorstep.<br>it modestly waits for the thread to finish its work,<br>and then politely says \"Hello\".<br><br>",
  "s1.p11": "And all that distinguishes it from a normal function is the word **⟦f4⟧**.",
  "s1.p12": "Remove that word, and \"Hello\" appears between \"Start...\" and \"Finish...\".",
  "s1.p13": "Why?",
  "s1.p14": "Because the **⟦f3⟧** function call returned a promise to say hello, but when it is convenient for the main thread to do so ![ico-20 smile]",
  "s2.h1": "More than just a promise",
  "s2.p1": "Sometimes you may want to streamline the execution of several asynchronous operations.",
  "s2.p2": "We can already solve such a problem with the ⟦f0⟧ instance and the call chain of **⟦f5⟧** method:",
  "s2.p3": "In this example, after 1 second, the first instance of ⟦f0⟧ will resolve with the message ‘Hello’.",
  "s2.p4": "When it resolves, a new instance of ⟦f0⟧ will be created, which will also resolve after 1 second.",
  "s2.p5": "The second instance of ⟦f0⟧ will append the string ‘, baby’ to the message returned by the first instance of ⟦f0⟧.",
  "s2.p6": "Eventually, the chain of ⟦f0⟧ instances will resolve after 2 seconds, and the console will show ⟦f6⟧.",
  "s2.p7": "Let's modify the code a bit to keep track of time.",
  "s2.p8": "To do this, let's declare a helper function ⟦f7⟧:",
  "s2.p9": "Now the chain of method **⟦f5⟧** calls of ⟦f0⟧ instances will be simplified, while we will see an indication of the execution time in milliseconds:",
  "s2.p10": "Now we'll see in the console something like:",
  "s2.p11": "The asynchronous function is an alternative solution.",
  "s2.p12": "Let's declare an auxiliary function **⟦f9⟧**:",
  "s2.p13": "Now let's adjust the code of the **⟦f7⟧** function a bit, replacing ⟦f8⟧ with a call of the **⟦f9⟧** function:",
  "s2.p14": "and now declare the asynchronous function **⟦f20⟧**:",
  "s2.p15": "and call it by passing a **⟦f9⟧** callback through the **⟦f5⟧** method:",
  "s2.p16": "What we see from this example:",
  "s2.p17": "• instead of calling the **⟦f5⟧** method of the **⟦f0⟧** instance, the **⟦f10⟧** keyword of the asynchronous function is used;",
  "s2.p18": "• the code that follows the line with **⟦f10⟧** will be executed as if this code were executed in the callback function passed to **⟦f5⟧** method of the previous **⟦f0⟧** instance;",
  "s2.p19": "• the asynchronous function call returns an instance of **⟦f0⟧**, so you can only get the result of the asynchronous function using the **⟦f5⟧** method;",
  "s2.p20": "• in order to resolve an asynchronous function call with the result, there must be a **⟦f11⟧** operator in the body of the asynchronous function.",
  "s3.h1": "Queue Manager",
  "s3.p1": "The asynchronous function is an excellent queue organiser.",
  "s3.p2": "It strictly enforces the order of the microtask queue ![ico-20 smile].",
  "s3.p3": "Suppose we have a function **⟦f12⟧** that returns an instance of ⟦f0⟧,",
  "s3.p4": "and also binds to the **⟦f9⟧** colback the first argument passed to the **⟦f12⟧** function.",
  "s3.p5": "The second argument to the **⟦f12⟧** function is used to set the timer:",
  "s3.p6": "and the **⟦f9⟧** function:",
  "s3.p7": "Let's make three consecutive calls to the **⟦f12⟧** function:",
  "s3.p8": "As we can see, the colbacks are returned when the timer time has expired, not in the order they were called.",
  "s3.p9": "Now let's queue them using the asynchronous function **⟦f13⟧**:",
  "s3.p10": "Now they're strictly following the queue ![ico-20 smile]",
  "s4.h1": "Organiser of asynchronous processes",
  "s4.p1": "^^To see which logins are in the database, execute in the console:^^",
  "s4.p2": "Let's slightly rewrite the previous example.",
  "s4.p3": "Declare a **⟦f14⟧** function that will return an instance of **Promise**.",
  "s4.p4": "The **⟦f14⟧** function takes a **⟦f15⟧** object as an argument, and creates an ⟦f16⟧ element to enter the login of the user.",
  "s4.p5": "The anonymous function that is passed to the **Promise** constructor sets the ⟦f17⟧ event handler of the ⟦f16⟧ element,",
  "s4.p6": "which calls either **⟦f9⟧** or **⟦f18⟧** depending on what was entered into the ⟦f16⟧ field.",
  "s4.p7": "(whether the corresponding user is in the database).",
  "s4.p8": "Now let's create an asynchronous function **⟦f19⟧** that will make a request to the server, get the data and call the function **⟦f14⟧** and pass it the received data:",
  "s4.p9": "The only thing left to do is to call the function **getLogin**:",
  "s4.p10": "◘◘![ico-20 file] Function getLogin call◘◘",
  "s4.p11": "and don't forget to press **Enter** after entering the login.",
  "s4.spoiler1": "Full example code",
  "s4.p12": "[![ico-30 hw] Quiz](quiz/async )"
}
