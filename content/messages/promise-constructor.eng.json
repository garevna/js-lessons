{
  "s1.h1": "The Promise constructor",
  "s1.p1": "To gain a deeper understanding of how the Promise constructor works,",
  "s1.p2": "we can write our own code that does practically the same thing",
  "s1.p3": "Since Promise callbacks are microtasks, we will use **MutationObserver**",
  "s1.p4": "To do this, let’s recall what it is",
  "s1.p5": "An instance of **⟦f0⟧** monitors changes to DOM elements",
  "s1.p6": "It is easy to create such an instance using the **⟦f0⟧** constructor:",
  "s1.p7": "A callback function is passed as an argument to the **⟦f0⟧** constructor,",
  "s1.p8": "which will become a microtask when a mutation event occurs on the monitored DOM element",
  "s1.p9": "So, the created **observer** object has inherited the **⟦f1⟧** method,",
  "s1.p10": "which we can use to set up monitoring of mutations on any DOM element:",
  "s1.p11": "The object ⟦f2⟧ contains information for the observer on exactly what to monitor when this element changes",
  "s1.p12": "You can monitor the addition or removal of descendants, changes to the element’s text content, and so on",
  "s1.p13": "We will monitor the addition or removal of child elements,",
  "s1.p14": "including text nodes",
  "s1.p15": "using the following configuration:",
  "s1.p16": "Now we need to specify the elements whose changes we will monitor",
  "s1.p17": "The thing is that when creating a promise, the function passed to the **Promise** constructor must be executed immediately",
  "s1.p18": "However, within the body of the function, the callbacks **⟦f3⟧** | **⟦f4⟧** are called,",
  "s1.p19": "which have not yet been defined at the time the **Promise** instance is created",
  "s1.p20": "and are formal parameters of this function",
  "s1.p21": "Therefore, when calling this function in the **CustomPromise** constructor",
  "s1.p22": "we will pass our own callbacks to this function,",
  "s1.p23": "which will receive the result or an error and store them in some variables...",
  "s1.p24": "However, variables are not monitored by the **observer** instance –",
  "s1.p25": "it can only monitor mutations of DOM elements",
  "s1.p26": "Therefore, we will place the response returned by the function into the container elements",
  "s1.p27": "**⟦f5⟧** and **⟦f6⟧**:",
  "s1.p28": "and then set up observation for mutations of these elements:",
  "s1.p29": "Of course, these container elements need to be created and made invisible",
  "s1.p30": "To do this, we’ll create an inherited method of our **CustomPromise** constructor:",
  "s1.p31": "All that remains is to call this method in the constructor to create the required container elements:",
  "s2.h1": "Custom constructor",
  "s3.h1": "Example of use",
  "s3.p1": "Now let’s use the **CustomPromise** constructor to create a promise:",
  "s3.p2": "and pass in the callbacks:",
  "s3.p3": "[%%%Tests%%%](⟦f7⟧)"
}
