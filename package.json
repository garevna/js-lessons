{
  "name": "js-lessons",
  "version": "5.0.0",
  "license": "MIT",
  "description": "Web-development. Educational materials for the JS course.",
  "type": "module",
  "main": "./public",
  "scripts": {
    "clean": "node -e \"const fs=require('fs');['index.js','donate.js','main-menu.js','content.worker.js','icons.worker.js','service-worker.js'].forEach(f=>fs.rmSync('public/'+f,{force:true}));fs.rmSync('public/.cache',{recursive:true,force:true})\"",
    "dev": "webpack --mode development -w",
    "start": "node live-server.js",
    "prod": "webpack --mode production",
    "ls": "git ls-files --cached",
    "deploy": "git subtree push --prefix public origin gh-pages",
    "staging": "git subtree push --prefix public staging gh-pages",
    "icons-worker": "cd icons-worker && npm run prod",
    "content-worker": "node content-worker/build-content.js && cd content-worker && npm run prod",
    "service-worker": "node service-worker/config-service-worker.js && cd service-worker && npm run prod",
    "content-only": "npm run content-worker && npm run service-worker",
    "scripts-only": "npm run prod && npm run service-worker",
    "content-scripts": "npm run prod && npm run content-worker && npm run service-worker",
    "full": "npm run icons-worker && npm run content-worker && npm run prod && npm run service-worker",
    "install:all": "yarn install --frozen-lockfile && cd icons-worker && yarn install --frozen-lockfile && cd ../content-worker && yarn install --frozen-lockfile && cd ../service-worker && yarn install --frozen-lockfile"
  },
  "keywords": [
    "javascript",
    "courseware",
    "lessons",
    "development",
    "client side"
  ],
  "author": "garevna <irina.h.fylyppova@gmail.com>",
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "css-loader": "^6.10.0",
    "live-server": "^1.2.2",
    "style-loader": "^3.3.4",
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4"
  }
}
