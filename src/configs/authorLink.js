/**
 * Irina's CV, linked quietly from the footer.
 *
 * The site is teaching material, not a portfolio, so the CV is not a page
 * here — it lives in its own repository and the footer only points at it.
 */
export const authorLink = 'https://github.com/garevna/cv-resume'
